PORT=3001
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gr_dev?schema=public"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-to-a-long-random-string-at-least-32-chars"
ENCRYPTION_KEY="your-32+-char-secret-for-encryption-service-min-32!!"
# Comma-separated if you need multiple (e.g. http://localhost:3000,https://your-app.ngrok-free.dev).
# Development: main.ts also allows *.ngrok-free.app / *.ngrok-free.dev via regex.
FRONTEND_URL="http://localhost:3000"
SMTP_HOST=localhost
SMTP_PORT=1025
MAIL_FROM="Polapine Clone <noreply@localhost>"
STRIPE_WEBHOOK_SECRET=
NODE_ENV=development
SEED_ADMIN_EMAIL=admin@localhost.test
SEED_ADMIN_PASSWORD=adminadminadmin
